"use strict";

/** @type {import('@adonisjs/framework/src/Env')} */
const Env = use("Env");

module.exports = {
  date: {
    date_default_format: "YYYY-MM-DD",
    hour_default_format: "HH:mm",
  },
};
